---
eigenvalue: "0xA055CC248B7649CC"
vector: 00.05.01
symbol: "C_ADD"
desc: "Church Encoding: Addition function. C_ADD m n = f => x => m(f)(n(f)(x))"
---

## GREEN (G)

Addition for Church Numerals. Sums `m` and `n` by chaining their application:
applies `f` `n` times to `x`, and then applies `f` `m` times to the result.

## BLUE (B)

```typescript
/**
 * C_ADD: Church Addition
 */
export const ATOM = () => (m: any) => (n: any) => (f: any) => (x: any) =>
  m(f)(n(f)(x));
```
