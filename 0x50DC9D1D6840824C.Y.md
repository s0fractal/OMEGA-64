---
eigenvalue: "0x50DC9D1D6840824C"
vector: 03.07.01
symbol: "Y"
desc: "Fixed-Point Combinator (Y). Enables recursion in anonymous functions."
---

## GREEN (G)

The Y Combinator. Enables recursion within the system by finding the fixed point
of a function. Crucial for building recursive machines and stream processors.

## BLUE (B)

```typescript
/**
 * Y: Fixed-Point Combinator
 */
export const ATOM = () => (f: any): any =>
  ((g: any) => g(g))((g: any) => f((x: any) => g(g)(x)));
```
