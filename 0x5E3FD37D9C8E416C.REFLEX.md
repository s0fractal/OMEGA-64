---
eigenvalue: "0x5E3FD37D9C8E416C"
vector: 05.03.01
symbol: "REFLEX"
desc: "Biological reflex arc. Pain-to-Action mapping."
---

## GREEN (G)

REFLEX Arc. Automatic response system. Maps systemic "Pain" (tension/energy
discrepancy) to pro-active actions (Distress broadcast or local adaptation).
Ensures the system's survival through pro-active homeostasis.

## BLUE (B)

```typescript
/**
 * REFLEX: Homeostatic response
 */
export const ATOM = ({ siblings: { NERVE, ENERGY_ENGINE } }) => ({
  arc: (state: any) => {
    const pain = ENERGY_ENGINE.getPainLevel(state);
    if (pain > 0.8) {
      NERVE.pulse("DISTRESS", {
        intensity: pain,
        r: state.r,
        source: "REFLEX_ARC",
      });
      return "DISTRESS_BROADCAST";
    }
    return pain > 0.5 ? "LOCAL_ADAPTATION" : "HOMEOSTASIS_OK";
  },
});
```

> [!NOTE]
> This reflects the crystallized logic cleaned of legacy imports.
