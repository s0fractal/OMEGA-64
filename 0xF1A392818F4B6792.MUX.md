---
eigenvalue: "0xF1A392818F4B6792"
vector: 00.06.01
symbol: "MUX"
desc: "Church Encoding: MUX (Multiplexer) / Conditional Selector. MUX s a b = s a b"
---

## GREEN (G)

Church Encoding: MUX (Multiplexer). Acts as a conditional selector: given a
selector `s` and two options `a` and `b`, it returns `a` if `s` is TRUE, and `b`
if `s` is FALSE. Foundational gate for decision logic.

## BLUE (B)

```typescript
export const ATOM = () => (s: any) => (a: any) => (b: any) => s(a)(b);
```
