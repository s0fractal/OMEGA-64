---
eigenvalue: "0xCA809C585FB51A04"
vector: 04.03.01
symbol: "ACTOR"
desc: "Autonomous Actor. (state => handler => message => handler(state)(message))"
---

## GREEN (G)

The ACTOR. A self-contained unit of logic and state that communicates via
asynchronous message passing. The foundation of the OMEGA-64 swarm intelligence.

## BLUE (B)

```typescript
/**
 * ACTOR: Autonomous Unit
 */
export const ATOM = () => (state: any) => (handler: any) => (msg: any) =>
  handler(state)(msg);
```
