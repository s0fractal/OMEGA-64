---
eigenvalue: "0xA4354D9D41A29B57"
vector: 00.05.00
symbol: "N0"
desc: "Church Numeral: Zero. (f => x => x)"
---

## GREEN (G)

Church Numeral: Zero. The base case for all Peano-style arithmetic in the lambda
calculus. Essentially equivalent to the B0 (FALSE) selector: it ignores the
function `f` and returns the base `x`.

## BLUE (B)

```typescript
/**
 * N0: Zero
 */
export const ATOM = () => (f: any) => (x: any) => x;
```
