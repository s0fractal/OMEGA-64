---
eigenvalue: "0xB38F9ABDA5C6752C"
vector: 07.03.01
symbol: "RADIANCE"
desc: "Radiant intensity. power / (area + 1)"
---

## GREEN (G)

Radiant Field. Calculates the intensity of a field at a given area based on its
power. Used for modeling the spread of influence or light in the Flatland.

## BLUE (B)

```typescript
/**
 * RADIANCE: Power Density
 */
export const ATOM = () => (power: any) => (area: any) => power / (area + 1);
```
