# i.L99.core.MOUNT_MANIFEST.md

# OMEGA-64 | Mount Manifest (Preview)

Purpose:

- Explain the dry mount projection output (`OMEGA_MOUNTS.json`).

Generated By:

- `deno task mount:scan`

Schema (example):

```json
{
  "generated_at": "2026-02-16T01:23:45.000Z",
  "source": "i.L99.core.MOUNT_LIST.md",
  "mounts": [
    {
      "mount_id": "ext.std",
      "source": "https://deno.land/std",
      "root": "/",
      "prefix": "i.ext.std",
      "mode": "lazy",
      "trust": "readonly",
      "files": [
        "i.ext.std.fs",
        "i.ext.std.path"
      ]
    }
  ]
}
```

Notes:

- `files` is a dot-fold projection list (virtual IDs).
- Empty `files` means the mount root was not available locally (expected for
  lazy mode).
