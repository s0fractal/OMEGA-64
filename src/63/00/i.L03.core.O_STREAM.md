# i.L03.core.O_STREAM.md

# OMEGA-64 | O_STREAM (Append-Only Output)

Purpose:

- Append-only stream of output proposals (O-space).

Definition:

- O_STREAM = append(proposal) → output_stream.

Notes:

- Output is noncanonical until collapsed.
- Ordering is causal, not temporal.
