# i.L99.core.O_STREAM_DRAIN_RUN.md

# OMEGA-64 | O_STREAM_DRAIN_RUN (Process)

Purpose:

- Drain O stream by removing consumed proposal IDs.

Notes:

- Noncanonical operator tool.
- Usage: deno run -A i.L99.core.O_STREAM_DRAIN_RUN.ts --input <ids.json>
  [--stream <path>]
