# i.L99.core.O_STREAM_TAG_ENFORCE_RUN.md

# OMEGA-64 | O_STREAM_TAG_ENFORCE_RUN (Process)

Purpose:

- Enforce tag policy on proposals.

Notes:

- Noncanonical operator tool.
- Usage: deno run -A i.L99.core.O_STREAM_TAG_ENFORCE_RUN.ts --input
  <stream.jsonl|list.json> [--pretty]
