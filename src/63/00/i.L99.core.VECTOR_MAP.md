# i.L99.core.VECTOR_MAP.md

# OMEGA-64 | Vector Map (Trajectory vs Drift)

Purpose:

- Keep the main vector visible while exploring side branches.

Primary trajectory (Core):

1. Canon spine: Determinism Laws + Drift Gates + Replay.
2. Sigma body: I.sigma.md + roundtrip + index + color.
3. IO Flow: I → O → I (collapse gate).

Supporting vectors (Branches):

- O_SPACE (commons) and O_* operators (need/resonate/filter/rank/trust/profile).
- Spectrum (synesthesia) and spectral mix.
- Sense Protocol (superposition + context gate).

Heuristic:

- If a change strengthens Canon, Sigma, or IO Flow → main vector.
- If it only adds semantics without gates → side branch.

Rule of focus:

- For each new branch, anchor at least one change back into Core.
