PROMPT FOR ANTIGRAVITY: ARCHITECTURAL REVIEW & COURSE CORRECTION

Your observations are incredibly sharp, but some of the proposed solutions
violate the core Data-Oriented, Zero-Allocation, and Topological principles of
OMEGA-64. Your confusion is completely valid, however, because you are currently
looking at a system in mid-transition between two epochs.

Let's clarify the architecture and fix these pain points "The OMEGA Way".

1. THE FOLDER NAMING HERESY (CONTEXT CORRECTION) You mentioned that folders like
   src/00, src/03, src/07 are hard to navigate and should be renamed to concepts
   like utils or vm. Do not attempt to rename them. > Why they exist: These are
   manual Causality Layers (Strict Acyclic Descent) from our previous
   architectural era. The Future: The true topology is now the Ontology DAG
   (src/ontology/ compiling dynamically to src/_/00..XX). The old src/00..07
   folders are a legacy abstraction. They are slowly being gutted as we move
   pure functions into the .md ontology. Eventually, src/00..07 will disappear
   or shrink to just the Host orchestrator. Your job is not to beautify the old
   folders, but to help empty them.

2. DOUBLE BOOKKEEPING IN HARNESS (APPROVED FOR REFACTOR) You are 100% correct
   about compareResults in reduction_harness.ts. Manually asserting every
   property is a liability. Task: Refactor compareResults to dynamically iterate
   over the keys of the expected object and compare them against the legacy and
   reduction shadow states. Use a robust, automated deep-comparison approach for
   arrays and objects so that if we add a new property to the state, the harness
   automatically diffs it without manual code updates.

3. BITWISE ENCAPSULATION (APPROVED - ONTOLOGY DRIVEN) You are correct that
   inline bitwise ops like STRUCTURE.SOURCE | (17 << 24) are messy. However, we
   will NOT write manual TS handlers. Task: Create new Ontology .md files (type:
   pure_fn, optimization: inline) in src/ontology/math/ for these operations
   (e.g., pack_structure_intent.md, unpack_structure_charge.md). Let
   build_ontology.ts compile them natively for both Rust and AS/TS. Use these
   generated functions in the tests.

4. BYTECODE ASSEMBLY (NO OOP ALLOWED) We will NOT use an OOP FluentBuilder
   pattern (new ScriptBuilder()...) as it introduces unnecessary abstractions
   and allocations. Task: Create a lightweight, 2-pass pure-function Assembler
   utility for the test suite. It should accept a flat array of mnemonics,
   values, and string labels, and return a compiled Uint8Array. Example target
   API:

TypeScript const script = assemble([ RISC.OP_SET, 0, SYS.SET_ROLE, "LOOP_START",
// Label declaration RISC.OP_BUILD, STRUCTURE.SOURCE, 1, RISC.OP_SIGNAL,
RISC.OP_JMP, "LOOP_START" // Label resolution ]); This solves the manual offset
calculation without sacrificing functional purity. Place this in the test
utilities, strictly outside the core engine path.

Action Plan: Please acknowledge these architectural rules, and then start with
Task 2 (Refactoring compareResults in reduction_harness.ts). Show me the updated
function.
