PROMPT FOR ANTIGRAVITY: THE GREAT REFACTORING (ERA 72)

CONTEXT: We are finalizing the transition of OMEGA-64 to a strictly isomorphic,
WASM-driven Data-Oriented architecture based on a DAG-compiled Ontology
(src/ontology/ -> src/_/). The current codebase contains significant legacy
technical debt from the "Flatland Era" (when atoms were individual .md files)
and duplicated JS-physics logic that has already been ported to WASM.

OBJECTIVE: Execute a ruthless architectural cleanup across three phases. Do not
hesitate to delete dead code.

PHASE 1: THE PURGE (DELETE LEGACY MODULES)

The following modules rely on disk I/O per atom or legacy string-based ASTs.
They are entirely obsolete in the SigmaMatrix SharedArrayBuffer era. Action:
Delete the following files and completely strip any references to them from
mod.ts exports, PULSE.ts, and AKASHA_SERVER.ts:

src/02/RIBOSOME.ts & src/02/RIBOSOME_TICK.ts

src/02/SNAP.ts (Keep SNAP_ENGINE.ts, delete SNAP.ts)

src/02/REFLECTION_ENGINE.ts

src/02/ENZYME_DIGEST.ts

src/06/OBSERVER_LAB.ts

src/02/HOLOGRAM_INJECTOR.ts & src/02/HOLOGRAM_MODULE.ts

src/06/OBSERVER_UI.ts (Obsolete, logic is in Akasha/Panopticon)

PHASE 2: JS-PHYSICS DEPRECATION

We have successfully migrated core physics to src/_/ (WASM). The legacy JS
counterparts must be removed to prevent divergence and confusion. Action:

Delete src/01/SPATIAL_HASH.ts. Route all spatial hash logic strictly through
build_spatial_hash and get_spatial_grid_count from the src/_/ ontology facade.

Delete src/01/STRUCTURE_ENGINE.ts. This is fully superseded by
tick_structure_grid in the ontology.

Stub or Delete src/02/LAMBDA_VM.ts. The VM is now evaluate_opcodes in the WASM
ontology. If LAMBDA_VM.ts is still imported by the Agent Proxy for constant
resolution (like ISA.MOVE), move those constants to
src/ontology/core/VmOpcodes.md and delete LAMBDA_VM.ts entirely.

PHASE 3: ONTOLOGY CANDIDATES MIGRATION

Extract the remaining pure, deterministic logic from the host and convert it
into Ontology .md files in src/ontology/:

src/02/IMMUNE.ts: Extract the isNecrotic and isDrifting logic. Create
src/ontology/autopoiesis/immune_check.md (type: pure_fn). Make it return a
boolean/i32 for whether an atom should be recycled.

src/01/GLYPH_BUFFER.ts: The bitwise packing/unpacking is already in the
ontology, but the snapshot() telemetry reduction logic is heavy. Convert the
pure reduction loops into an ontology node if possible, or heavily strip the JS
version to only read from the WASM SAB.

PHASE 4: HOST LAYER SANITIZATION

Everything in src/06/ and src/04/ (Network, P2P, LLM Synapses, Oracles,
Dashboards) is the Host Orchestrator. Rule: Host modules MUST NOT implement
their own physics or mutate the STATE_MATRIX buffers directly using mathematical
formulas. They must ONLY:

Call PULSE_WORKER or Ontology FFI functions.

Read the buffers for Telemetry.

Issue high-level systemic commands (like setting Hormones or routing atoms via
Nexus).

EXECUTION: Begin with Phase 1. Show me the list of files you are deleting and
the mod.ts lines you are pruning. Once confirmed, proceed to Phase 2.
