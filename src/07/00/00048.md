### ТЗ для АГЕНТА: `00048.md`

```markdown
# Phase 55: Matrix 64 (Pure Octal Topology & Absolute Root)

## Контекст (Context)
Ми еволюціонуємо від семантичних папок до чистої двовимірної векторної топології (Фазовий Простір). Система стає 64-клітинною матрицею (8x8).
Усі імена папок повинні стати суворими двозначними числами (з нулем спереду: `00`, `01`... `07`, `08`, `63`). Жодних текстових суфіксів. Резолв шляхів переходить на рівень "Таблиці сторінок" (Page Table) у `deno.jsonc`.

## Крок 1: Трансмутація першого рівня (Root Vectors)
Перейменуй головні директорії, відсікши всі текстові слова:
- `00_substrate/` ➡️ `00/`
- `01_physics/` ➡️ `01/`
- `02_metabolism/` ➡️ `02/`
- `03_governance/` ➡️ `03/`
- `04_noosphere/` ➡️ `04/`
- `05_exocortex/` ➡️ `05/`
- `06_akasha/` ➡️ `06/`
- `07_meta/` ➡️ `07/`
- `08_artifacts/` ➡️ `08/`
- `63_necropolis/` ➡️ `63/`

## Крок 2: Фрактальна типізація (Sub-Vectors)
Всередині кожного `XX/` перейменуй підпапки на їхні логічні числові відповідники (за вісімковим словником OMEGA). 
*Приклад: `07_meta/02_runners/` стає `07/02/`. `00_substrate/03_tests/` стає `00/03/`.*

## Крок 2.5: Правило Двох та Суверенні Території (Flattening with Exceptions)
Максимальна глибина вкладеності директорій для нашого Deno/TS коду — рівно ДВА рівні (`XX/YY/`). Усередині цих координат файли лежать плоско.

**КРИТИЧНИЙ ВИНЯТОК (SOVEREIGN TERRITORIES):**
Ніколи не застосовуй "сплющення" і не змінюй внутрішню структуру до проєктів, які керуються зовнішніми компіляторами (Rust/Cargo, Node тощо).
Папка з Rust-ядром (колишня `00_substrate/sigma_core/`, тепер `00/00/00/sigma_core/` або `00/01/sigma_core/` залежно від того, куди ти її покладеш) є **Суверенною Територією**.
Вона ПОВИННА зберегти свою внутрішню ієрархію:
- `sigma_core/Cargo.toml`
- `sigma_core/src/**/*.rs`
- `sigma_core/.cargo/`
НЕ ЧІПАЙ внутрішню структуру `sigma_core/` та папки `public/`, `ui/` (якщо вони є).

## Крок 3: Стерилізація Кореня (Absolute Root Purity)
У кореневій директорії проєкту не має залишитися жодної логіки чи `.ts` файлів. 
1. Перемісти `SYSTEM_START.ts` у `07/02/SYSTEM_START.ts` (Runner).
2. Залишки скриптів рознеси по векторах (напр. генератори в `07/05/`).
3. У корені залишаються ЛИШЕ: `deno.jsonc`, `.env` (та `.env.example`), `.gitignore`, і сімлінк `README.md`.
4. Онови шляхи у секції `"tasks"` файлу `deno.jsonc`.

## Крок 4: Жорсткий мапінг регістрів (Explicit Page Table)
Онови `deno.jsonc`. Блок `imports` має містити ЯВНІ координати кожної активної клітинки матриці. Використовуй формат `@XX` для фасадів та `@XX/YY` для глибоких векторів.

## Крок 5: The Great Rewiring (Масова заміна імпортів)
Прожени масову заміну по всьому коду. Заміни всі відносні імпорти (`../../01_physics/mod.ts` або `@01`) на чисті вектори (`@01`). Якщо імпорт ішов з підпапки, використовуй повний вектор (напр. `import { ... } from "@07/02"`). Жодних брудних шляхів у `.ts` файлах.

## Крок 6: Клітинна Пам'ять (`mod.md`)
У кожній директорії `XX/` та `XX/YY/` створи/онови файл `mod.md`.

## Constraint Check
Запусти `deno task start` (чи еквівалентний базовий флоу). Система має працювати ідеально, без жодної помилки імпорту.
```
