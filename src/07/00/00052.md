PROMPT FOR ANTIGRAVITY: THE DETERMINISM FIREWALL

CONTEXT: We are enforcing absolute cross-platform determinism (WASM/V8/Rust
parity) in the lower layers of the OMEGA-64 Matrix engine. Non-deterministic
transcendental math (Math.sin, Math.cos), host-dependent random number
generators (Math.random), and cross-platform rounding anomalies (Math.round,
Math.floor) cause fatal butterfly-effect state desynchronizations across P2P
nodes.

OBJECTIVE: Implement a "Determinism Firewall" inside build_ontology.ts to
strictly quarantine non-deterministic math to the host layer.

ACTION: Update the build_ontology.ts script to include the following validation
step right after the Semantic Firewall check inside the computeLevel function:

1. Define the Blacklist: Create a RegExp pattern that detects forbidden tokens
   in the source code blocks. Forbidden tokens: Math.sin, Math.cos, Math.tan,
   Math.acos, Math.asin, Math.atan, Math.exp, Math.log, Math.round, Math.ceil,
   Math.floor, Math.random. (Note: Math.max, Math.min, Math.abs and Mathf.sqrt
   are currently exempt as they map safely to IEEE 754 WASM instructions, but
   bitwise fast_max/fast_min/fast_abs are highly preferred).

2. Implement the Rule: When processing a node (especially of type pure_fn),
   check its tags array.

IF the node has the tag "host" or "substrate": It is EXEMPT. Allow the math.

IF the node DOES NOT have the "host" or "substrate" tag: Scan its tsCode,
asCode, and rustCode strings against the Blacklist.

3. Enforcement (Panic): If a forbidden token is found in a pure node, the
   compiler MUST halt immediately with a fatal error: console.error("[FATAL]
   Determinism Breach in Node '<NODE_ID>': Detected non-deterministic host math
   ('<MATCHED_TOKEN>'). Pure functions must use integer math, LUTs, or bitwise
   ops. If this is intentional UI/Host logic, add 'host' to the node's tags.")
   Deno.exit(1);

4. Self-Correction in existing MD files: After updating the script, scan the
   current .md files in src/ontology/autopoiesis/. You will notice
   apply_metabolism_kernel.md uses Math.floor for integer division. Please
   update apply_metabolism_kernel.md to replace Math.floor(x / 2) with bitwise
   shifting (x >> 1) or standard integer division truncations to comply with the
   new brutalist firewall.
