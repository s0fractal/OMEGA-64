---
eigenvalue: "0x987A10662A40A900"
vector: 03.07.00
symbol: "CAR"
desc: "Church Encoding: CAR (Pair Head) / First element extraction."
---

## GREEN (G)

CAR (Contents of the Address part of the Register). Retrieves the first element
of a pair constructed with CONS. Applying a pair `p` to the TRUE selector (T).

## BLUE (B)

```typescript
/**
 * CAR: Head Selector
 */
export const ATOM = ({ siblings: { T } }) => (p: any) => p(T);
```
