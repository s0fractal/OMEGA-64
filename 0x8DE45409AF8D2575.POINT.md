---
eigenvalue: "0x8DE45409AF8D2575"
vector: 07.05.00
symbol: "POINT"
desc: "3D Point (Coordinate Triple). TRIPLE(x)(y)(z)"
---

## GREEN (G)

Spatial POINT. Represents a specific coordinate in the OMEGA-64 3D vector space.
Constructed using the Level 5 `TRIPLE` operator.

## BLUE (B)

```typescript
/**
 * POINT: 3D Coordinate
 */
export const ATOM =
  ({ siblings: { TRIPLE } }) => (x: any) => (y: any) => (z: any) =>
    TRIPLE(x)(y)(z);
```
