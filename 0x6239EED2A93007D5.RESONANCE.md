---
eigenvalue: "0x6239EED2A93007D5"
vector: 05.07.00
symbol: "RESONANCE"
desc: "Wave Resonance logic. (a === b ? WAVE_SIGNAL(a) : WAVE_SIGNAL(b))"
---

## GREEN (G)

Wave Resonance. Compares two signals `a` and `b`. If they match, it reinforces
the signal; otherwise, it dampens or shifts it. Ensures coherence across the
flatland vector space.

## BLUE (B)

```typescript
/**
 * RESONANCE: Signal Coherence
 */
export const ATOM =
  ({ siblings: { WAVE_SIGNAL } }) => (a: any) => (
    b: any,
  ) => (a === b ? WAVE_SIGNAL(a) : WAVE_SIGNAL(b));
```
