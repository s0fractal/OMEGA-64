---
eigenvalue: "0xD6EEABB40850072B"
vector: 02.02.00
symbol: "JUST"
desc: "Maybe Monad: JUST case. Wraps a value 'x' in a successful context."
---

## GREEN (G)

Maybe Monad: JUST. Represents a successful computation or present value. Wraps
`x` and provides it to the successful branch `j`.

## BLUE (B)

```typescript
export const ATOM = () => (x: any) => (_n: any) => (j: any) => j(x);
```
