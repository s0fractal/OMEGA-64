---
eigenvalue: "0xDF329926A82F9FC1"
vector: 07.07.01
symbol: "RIGHT"
desc: "Either/Pair Right projection. (y => _l => r => r(y))"
---

## GREEN (G)

Right projection for Either types or pairs. Extracts the right component of a
sum type.

## BLUE (B)

```typescript
/**
 * RIGHT: Sum-type selector (Right)
 */
export const ATOM = () => (y: any) => (_l: any) => (r: any) => r(y);
```
