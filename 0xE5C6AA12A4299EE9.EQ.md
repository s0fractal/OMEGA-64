---
eigenvalue: "0xE5C6AA12A4299EE9"
vector: 01.00.00
symbol: "EQ"
desc: "Church Encoding: Equality predicate. Checks if m == n."
---

## GREEN (G)

Numeric equality check via Church Encoding. Returns TRUE (T) if `m` is equal to
`n`, or FALSE (F) otherwise. Relies on LEQ (Less than or Equal).

## BLUE (B)

```typescript
/**
 * EQ: Church Encoding Equality
 */
export const ATOM = ({ siblings: { LEQ, T, F } }) => (m: any) => (n: any) =>
  LEQ(m)(n)(LEQ(n)(m))(F);
```
