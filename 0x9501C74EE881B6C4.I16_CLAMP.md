---
eigenvalue: "0x9501C74EE881B6C4"
vector: 00.00.09
symbol: "I16_CLAMP"
desc: "Foundational Signed 16-bit Clamping utility. Prevents values from exceeding I16 limits."
---

## GREEN (G)

I16_CLAMP ensures that a signal stays within the legal -32768 to 32767 range.
Essential for somatic logic and wave interference stability.

## BLUE (B)

```typescript
/**
 * I16_CLAMP: Foundational Signed 16-bit Clamping
 */
export const ATOM = ({ siblings: { I16_LIMITS } }) => (x: number) => {
  const limits = I16_LIMITS();
  return x > limits.max ? limits.max : (x < limits.min ? limits.min : x);
};
```
