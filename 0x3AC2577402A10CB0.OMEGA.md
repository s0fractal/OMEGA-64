---
eigenvalue: "0x3AC2577402A10CB0"
vector: 07.07.00
symbol: "OMEGA"
desc: "The Ouroboros Link. Feeds system output back into the interface."
---

## GREEN (G)

The OMEGA operator. The point of loop closure. It takes the entire system
`lattice` and projects it through the `INTERFACE`. "The End is the Beginning."

## BLUE (B)

```typescript
/**
 * OMEGA: Systemic Recursion
 */
export const ATOM = ({ siblings: { INTERFACE } }) => (lattice: any) =>
  INTERFACE(lattice);
```
