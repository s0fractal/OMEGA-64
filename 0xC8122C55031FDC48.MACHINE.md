---
eigenvalue: "0xC8122C55031FDC48"
vector: 03.00.00
symbol: "MACHINE"
desc: "State Machine constructor. MACHINE transition state = pairs them."
---

## GREEN (G)

State Machine constructor. Pairs a transition function with an initial state.
The foundational structure for all autonomous behavior in OMEGA-64.

## BLUE (B)

```typescript
/**
 * MACHINE: State/Control Unit
 */
export const ATOM = () => (transition: any) => (state: any) => (pair: any) =>
  pair(transition)(state);
```
