---
eigenvalue: "0xD3CB93F33153FFF2"
vector: 03.00.00
symbol: "HALT"
desc: "Machine Halt state. Terminates processing by returning only state."
---

## GREEN (G)

Machine Halt state. Represents a terminal configuration where any further input
`i` is ignored, and the current state `s` is returned. `HALT s i = s`.

## BLUE (B)

```typescript
/**
 * HALT: Terminal Transition
 */
export const ATOM = () => (s: any) => (_i: any) => s;
```
