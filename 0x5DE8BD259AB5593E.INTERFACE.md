---
eigenvalue: "0x5DE8BD259AB5593E"
vector: 07.07.00
symbol: "INTERFACE"
desc: "System Interface Membrane. Wraps data in a semantic tag."
---

## GREEN (G)

Universal Interface. Wraps an input `x` in a semantic membrane with the tag
`"RAW"`. Foundational for data-to-meaning transitions and I/O boundary
definitions.

## BLUE (B)

```typescript
/**
 * INTERFACE: Semantic encapsulation
 */
export const ATOM = ({ siblings: { CONS } }) => (x: any) => CONS(x)("RAW");
```
