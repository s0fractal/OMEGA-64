---
eigenvalue: "0x85B23CEA5D89D1C4"
vector: 03.07.01
symbol: "OR"
desc: "Church Encoding: Logical OR. OR p q = p p q"
---

## GREEN (G)

Logical OR operator via Church Encoding. Returns TRUE if at least one of its
arguments is TRUE. Optimized implementation: `OR p q = p p q`.

## BLUE (B)

```typescript
/**
 * OR: Logical Disjunction
 */
export const ATOM = () => (p: any) => (q: any) => p(p)(q);
```
