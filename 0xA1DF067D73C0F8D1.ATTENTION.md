---
eigenvalue: "0xA1DF067D73C0F8D1"
vector: 06.06.00
symbol: "ATTENTION"
desc: "Filtered sensation. filter(p) ? f(p) : FORCE(p)"
---

## GREEN (G)

Cognitive ATTENTION. A high-level filter that focuses processing resources on
specific sensations. If the sensation `p` passes the `filter`, function `f` is
applied; otherwise, a default `FORCE` is maintained.

## BLUE (B)

```typescript
/**
 * ATTENTION: Selective Filter
 */
export const ATOM =
  ({ siblings: { FORCE } }) => (f: any) => (filter: any) => (p: any) =>
    filter(p) ? f(p) : FORCE(p);
```
