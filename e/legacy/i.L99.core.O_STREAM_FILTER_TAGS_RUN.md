# i.L99.core.O_STREAM_FILTER_TAGS_RUN.md

# OMEGA-64 | O_STREAM_FILTER_TAGS_RUN (Process)

Purpose:

- Filter O stream proposals by tags.

Notes:

- Noncanonical operator tool.
- Usage: deno run -A i.L99.core.O_STREAM_FILTER_TAGS_RUN.ts --input
  <stream.jsonl|list.json> --tags tagA,tagB [--pretty]
