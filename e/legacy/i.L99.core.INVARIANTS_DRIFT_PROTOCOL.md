# i.L99.core.INVARIANTS_DRIFT_PROTOCOL.md

# OMEGA-64 | Invariants & Drift Control Protocol

# Purpose: prepare lattice for autonomous recursion without grey-goo drift.

Context:

- This protocol defines non-negotiable invariants and drift controls.
- It is enforced by MUTATE, CRYSTALLIZATION, REPLAY_AUDIT, and DETERMINISM_LAWS.
- It is a canon document, not a style guide.

Hard invariants (Diamond Core):

- 64-level hierarchy is fixed. No L65 creation, no L33 deletion.
- Sigma vector sum is a canonical function (calculate_sigma is invariant).
- Genesis hash anchor must remain unchanged (consensus reality).

Meta-invariants (Rules for rules):

- Mutation velocity cap: no more than 5 percent of codebase per tick.
- Semantic preservation: public interface must remain compatible.
- Rollback guarantee: every mutation must be revertible (undo path).

Generation rules (Drift control for LLM/Deep Think):

- Entropy bounds: 0.8 * parent_entropy < child_entropy < 1.2 * parent_entropy.
- Anchor keywords: each generated file must contain lattice anchors (e.g.
  Lattice, Vector, Recursion).

Enforcement mapping:

- DETERMINISM_LAWS: address=essence, fixed-point only, no forbidden IO.
- PROJECTION_DRIFT_ANALYTICS: drift and drift-slope gates for crystallization.
- MUTATE: safe window requires replayGreen + drift thresholds.
- DRIFT_CONTROLLER: compares old/new source for entropy, imports, and types.

Notes:

- Hard invariants cannot change by consensus.
- Meta-invariants can evolve, but only through canonized mutation.
- Generation rules are required for autonomous agents and Deep Think.
