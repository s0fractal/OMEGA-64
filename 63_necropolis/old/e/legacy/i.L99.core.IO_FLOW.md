# i.L99.core.IO_FLOW.md

# OMEGA-64 | IO_FLOW (I → O → I)

Purpose:

- Canonical collapse operator from Output stream to next Input state.

Definition:

- I: StateSnapshot (canonical state).
- O: output_stream (DeltaProposal[]).
- Collapse: IO_FLOW → GLIDER_LITE → GATE.

Notes:

- IO_FLOW is a topology operator, not a UI.
- Ledger emission is handled by GATE.
