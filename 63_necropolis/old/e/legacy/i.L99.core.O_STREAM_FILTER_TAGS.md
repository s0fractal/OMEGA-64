# i.L99.core.O_STREAM_FILTER_TAGS.md

# OMEGA-64 | O_STREAM_FILTER_TAGS (Filter)

Purpose:

- Filter proposals by required tag set.

Notes:

- Proposals without tags are excluded when filter is non-empty.
