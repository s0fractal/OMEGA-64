# i.L99.core.O_STREAM_APPEND_RUN.md

# OMEGA-64 | O_STREAM_APPEND_RUN (Process)

Purpose:

- Minimal CLI process for appending proposals to O stream.

Notes:

- Noncanonical operator tool.
- Usage: deno run -A i.L99.core.O_STREAM_APPEND_RUN.ts --input <proposal.json>
  [--stream <path>]
