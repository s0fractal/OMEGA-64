---
eigenvalue: "0xB576E8861629E7F6"
vector: 00.06.01
symbol: "XOR"
desc: "Church Encoding: XOR logic. Computes the exclusive OR of two signals."
---

## GREEN (G)

Church Encoding: XOR (Exclusive OR). Returns TRUE if and only if the inputs are
different. Uses the NOT operator for its implementation.

## BLUE (B)

```typescript
export const ATOM = ({ siblings: { NOT } }) => (p: any) => (q: any) =>
  p(NOT(q))(q);
```
