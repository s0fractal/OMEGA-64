---
eigenvalue: "0x902EB8AD9E956A2C"
vector: 05.07.00
symbol: "ETHER"
desc: "Wave Medium. Passes itself to a receiver function. (f => f(WAVE_SIGNAL))"
---

## GREEN (G)

The ETHER. Represents the underlying medium through which wave signals
propagate. When applied to a function `f`, it injects the `WAVE_SIGNAL` identity
into it.

## BLUE (B)

```typescript
/**
 * ETHER: Propagation Medium
 */
export const ATOM = ({ siblings: { WAVE_SIGNAL } }) => (f: any) =>
  f(WAVE_SIGNAL);
```
