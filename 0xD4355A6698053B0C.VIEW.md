---
eigenvalue: "0xD4355A6698053B0C"
vector: 07.05.00
symbol: "VIEW"
desc: "Lens viewer. Projects a structure through a lens. (l => struct => l(g => s => g(struct)))"
---

## GREEN (G)

The VIEW. Projective operator that applies a lens `l` to a structure `struct`.
Allows semantic observing of complex data without mutation.

## BLUE (B)

```typescript
/**
 * VIEW: Sematic Observer
 */
export const ATOM = () => (l: any) => (struct: any) =>
  l((g: any) => (_s: any) => g(struct));
```
