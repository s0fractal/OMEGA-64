---
eigenvalue: "0xD90E147CD4D6399A"
vector: 01.00.00
symbol: "SUB"
desc: "Church Encoding: Subtraction function. SUB m n = n PRED m"
---

## GREEN (G)

Subtraction operation via Church Encoding. Iteratively applies the PRED
(Predecessor) function `n` times to `m`.

## BLUE (B)

```typescript
/**
 * SUB: Church Encoding Subtraction
 */
export const ATOM = ({ siblings: { PRED } }) => (m: any) => (n: any) =>
  n(PRED)(m);
```
