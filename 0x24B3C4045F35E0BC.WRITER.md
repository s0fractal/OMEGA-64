---
eigenvalue: "0x24B3C4045F35E0BC"
vector: 03.02.00
symbol: "WRITER"
desc: "Writer Monad: Value and Output pair. WRITER a w = pairs them."
---

## GREEN (G)

Writer Monad constructor. Encapsulates a computational result `a` along with
accumulated side-output (log/buffer) `w`.

## BLUE (B)

```typescript
/**
 * WRITER: Output-bearing context
 */
export const ATOM = () => (a: any) => (w: any) => (pair: any) => pair(a)(w);
```
