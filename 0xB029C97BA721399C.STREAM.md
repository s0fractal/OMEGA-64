---
eigenvalue: "0xB029C97BA721399C"
vector: 01.07.00
symbol: "STREAM"
desc: "Lazy Stream constructor. STREAM head tailThunk = CONS head tailThunk"
---

## GREEN (G)

Lazy Stream constructor. Builds a stream by pairing a `head` value with a
`tailThunk` (a function that yields the rest of the stream when called). The
basis for infinite and lazy data structures.

## BLUE (B)

```typescript
/**
 * STREAM: Lazy Data Pipe
 */
export const ATOM =
  ({ siblings: { CONS } }) => (head: any) => (tailThunk: any) =>
    CONS(head)(tailThunk);
```
