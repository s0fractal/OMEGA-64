---
eigenvalue: "0x5E03208C5CCB80CE"
vector: 07.03.01
symbol: "COMM"
desc: "Communication bridge. (is => m => is(s1 => s2 => m))"
---

## GREEN (G)

Communication Bridge. A meta-operator that establishes a message `m` delivery
between two identities via an `is` (isomorphism/empathy) check.

## BLUE (B)

```typescript
/**
 * COMM: Message Bridge
 */
export const ATOM = () => (is: any) => (m: any) =>
  is((s1: any) => (s2: any) => m);
```
